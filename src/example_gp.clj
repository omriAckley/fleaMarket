(ns example-gp)

#_(use '(incanter core stats charts datasets))

#_(with-data (get-dataset :iris)
           (view (bar-chart $data)))

#_(view $data)

#_(def super-flea
  {:fleas {}
   :information {}
   :holdings {}
   :determine-relative-value_expr '()
   :relative-values {}})
